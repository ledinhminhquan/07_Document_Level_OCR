# Full local/dev install (everything). For a minimal core install use `pip install -e .`
# and add extras: `.[ml]`, `.[ocr]`, `.[api]`, `.[report]`, or `.[all]`.

# ── core ─────────────────────────────────────────────────────────────────────
numpy>=1.23
pyyaml>=6.0
pydantic>=2.0
pillow>=9.5

# ── trainable model (ByT5/T5 post-OCR corrector) ────────────────────────────
torch>=2.1
transformers>=4.40
datasets>=2.18
accelerate>=0.30
sentencepiece>=0.1.99
evaluate>=0.4
jiwer>=3.0

# ── OCR front-end + layout + PDF ────────────────────────────────────────────
pytesseract>=0.3.10          # needs the system `tesseract` binary (+ tesseract-ocr-eng)
pymupdf>=1.24                # born-digital PDF text + rasterize
opencv-python-headless>=4.8
pdf2image>=1.17              # needs poppler-utils
symspellpy>=6.7             # SymSpell dictionary baseline

# ── serving (API + UI) ──────────────────────────────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
gradio>=4.0

# ── reporting (auto report.pdf + slides.pptx) ───────────────────────────────
reportlab>=4.0
python-pptx>=0.6.23
matplotlib>=3.7

# ── optional higher-quality OCR engines / comparators ───────────────────────
# python-doctr>=0.8           # DBNet + CRNN/ViTSTR (weights auto-download from pip)
# easyocr>=1.7                # pure-python OCR fallback (no system binary)
# anthropic>=0.39             # optional LLM reasoning brain for the agent
